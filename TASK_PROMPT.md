# المهمة المطلوبة — مراجعة كود شاملة + خطة تطوير احترافية

> الصق ده في بداية الجلسة. (لو الجلسة مش هتحمل `MASTER_PROMPT.md` تلقائيًا، الصقه معاه — ده بيحدّد «إزاي تتصرف»، وده بيحدّد «إيه تعمل».)

---

## المطلوب منك بالظبط (The Mission)

اعمل حاجتين، بالترتيب ده، وسلّمهم كملفين منفصلين:

1. **مراجعة كود شاملة وموثّقة** للمشروع كله.
2. **خطة تطوير احترافية** بترتيب أولويات، بانية على المراجعة.

مفيش حاجة تانية مطلوبة. الهدف النهائي: تورّيني المشروع ده إيه وضعه الحقيقي دلوقتي، وإيه الترتيب الصح اللي نصلّح بيه المشاكل عشان نوصل لمستوى إصدار رسمي على متجر.

---

## إيه بالظبط اللي هتسلّمه (Deliverables)

### ملف 1: `CODE_REVIEW_<اليوم>.md`

- **ملخص تنفيذي** في أول 10 أسطر: أخطر 5 مشاكل في المشروع، كل واحدة بسطر.
- **جدول findings** بكل مشكلة لقيتها: `الملف | السطر | الخطورة (حرج/عالٍ/متوسط/منخفض) | الوصف | الاقتراح القابل للتنفيذ`.
- **ستة أقسام** — قسم لكل محور: الأمان، الأداء، قاعدة البيانات، حماية التطبيق، السلاسة، الوصولية.
- **قسم مستقل لمشاكل العربية/RTL** على الموبايل (الحروف المقصوصة والأبعاد الكبيرة).

### ملف 2: `DEV_PLAN_<اليوم>.md`

- **أولوية عالية**: إصلاحات الأمان وقاعدة البيانات (أي حاجة بتكسر الـ RPC/RLS أو بتسرّب بيانات).
- **أولوية متوسطة**: الأداء والتصميم والوصولية.
- **أولوية منخفضة**: تحسينات لطيفة (polish).
- كل بند لازم يحدّد: **الملفات اللي هتتلمس + خطوات التنفيذ + المخاطر + طريقة الرجوع (rollback)**.

---

## خطوات التنفيذ (هتعمل إيه بالظبط، بالترتيب ده)

1. اقرأ ملفات التحليل السابقة الأول ومتبدأش من الصفر:
   `AUDIT_COMPREHENSIVE_AR.md` · `PRODUCTION_AUDIT_AR.md` · `DESIGN_AUDIT_APPLE.md` · `IMPLEMENTATION_PLAN_APPLE_DESIGN.md` · `SUPABASE_SETUP.md`.

2. اعمل جرد للملفات المصدرية فعليًا:
   `src/app/` · `src/data/` · `src/design/` · `src/features/` · `src/i18n/` · `src/shared/` · `src/types/` — ومراجعة الـ migrations والسياسات في Supabase.

3. راجع الكود بنفسك (اقرأ، ما تختصرش من غير قراءة):
   - `src/data/engine.ts` (المرآة الحتمية) و `remote.ts` و `actions.ts` — تأكد إن كل الكتابة عبر RPCs بس.
   - كل RPC: هل `auth.uid()` بيتحقق فيه؟ هل الـ role بيتفحص؟ هل RLS متسق معه؟

4. شغّل أوامر التحقق وورّيني الناتج حرفيًا:
   ```bash
   npm run typecheck
   npm run parity
   npm run test:engine
   npm run test:rls
   npm run test:all
   npm run export:web
   ```

5. اكتب `CODE_REVIEW_<اليوم>.md` بناءً على اللي شفته فعليًا + نتايج الأوامر.

6. اكتب `DEV_PLAN_<اليوم>.md` بانية على المراجعة + ملفات التحليل السابقة.

---

## معايير القبول (متى تفتكر إنك خلصت)

- كل finding في المراجعة له: ملف + سطر + خطورة + اقتراح قابل للتنفيذ.
- مستحيل تكتب بند من غير ما تشوفه بعينك في الكود أو في نتايج الأوامر.
- الخطة فيها **على الأقل 5 بنود أولوية عالية**، وكل بند له ملفات محددة + خطوات + rollback.
- الناتج النهائي **بالعربي**، وأسماء الكود والمكتبات والتقنيات **بالإنجليزي**.
- **ممنوع** تدّعي إن حاجة اشتغلت من غير ما تشغّل الأوامر وتورّي الناتج. «مفيش أعتقد إنه شغال».

---

## قيود أمان لازم تحترمها وانت بتشتغل

- **كل الكتابة عبر RPCs فقط** — مفيش أي مسار كتابة مباشر من العميل. أي اقتراح يخالف ده = خطأ حرج.
- **`auth.uid()` بيتحقق في كل RPC**، و RLS متسق مع RPCs — أي فجوة هنا أولوية عالية فورًا.
- **الدخول Google OAuth فقط** — مفيش مسارات auth تانية.
- **مفيش بيانات وهمية** — كل قراءة/كتابة حقيقية عبر `supabase-js`.