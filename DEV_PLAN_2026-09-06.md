# DEV_PLAN_2026-09-06 — خطة التطوير (مسار 3.2)

> تحل محل `DEV_PLAN_2026-08-28.md` وتبني عليها.
> مبنية على `CODE_REVIEW_2026-09-06.md` و`docs/COMPETITIVE_ANALYSIS_2026-09.md`.
> **حالة التحقق:** `test:all` → 79 ✅ / 0 ❌ · `typecheck` ✅ · `parity` ✅ 658 · `export:web` ✅

---

## أولًا: ما نُفِّذ في هذه الجولة (مغلق ومُتحقَّق منه)

| # | البند | الملفات | التحقق |
|---|---|---|---|
| ✅1 | **تحديد معدل خادمي** لـ 4 RPCs حساسة | `supabase/migrations/0025_rpc_rate_limits.sql` | SQL متوازن، مولَّد في `WEB_EDITOR_UPGRADE_5.sql` |
| ✅2 | **أنبوب دفع الإشعارات كامل** (trigger → outbox → Edge Function → Expo) | `0026_push_delivery_outbox.sql`, `supabase/functions/push-dispatch/index.ts` | `FOR UPDATE SKIP LOCKED`، حذف تلقائي للتوكنات الميتة |
| ✅3 | **تفضيلات إشعارات لكل نوع** + واجهة | `0026`, `src/data/actions.ts`, `NotificationsScreen.tsx`, `i18n` | parity ✅ |
| ✅4 | **Geofence عميل حقيقي** (كان معطّلًا صامتًا) | `src/shared/location.ts`, `ScannerScreen.tsx`, `app.json` | typecheck ✅ |
| ✅5 | **توكن Push حقيقي** + قناة أندرويد + مستمعون | `src/shared/push.ts`, `src/data/store.tsx`, `app.json` | typecheck ✅ |
| ✅6 | **إصلاح قصّ العربية + سلم متجاوب** | `src/design/tokens.ts`, `src/design/components.tsx` | export:web ✅ |
| ✅7 | **وصولية**: 9 تسميات + `radio` + أهداف لمس 44pt | 5 ملفات في `src/features/` | typecheck ✅ |
| ✅8 | **حذف `src/data/domain.ts`** (419 سطر كود ميت بأعمدة خاطئة) | — | typecheck ✅ بعد الحذف |
| ✅9 | **تنظيف دوري** للجداول المنزلقة | `0026` (`masar-housekeeping`) | — |
| ✅10 | **تحليل تنافسي موثّق** | `docs/COMPETITIVE_ANALYSIS_2026-09.md` | 10 مصادر |
| ✅11 | **رسوم متجهة مخصصة لشاشات الترحيب (D1)** | `src/design/illustrations/*` | 3 مكونات SVG كاملة الدقة |
| ✅12 | **لون التمايز الثانوي Amber وحجم CTA بـ 52pt (D2, D5)** | `src/design/tokens.ts`, `AuthScreens.tsx` | Apple HIG + WCAG AA |
| ✅13 | **تصدير تقارير المنظمة CSV مع UTF-8 BOM (F9)** | `src/features/org/AdminScreens.tsx` | اختبار عملي وتصدير الفروع/الكورسات |
| ✅14 | **مشاركة بطاقة الإنجاز (F10)** | `src/design/celebrations.tsx` | مشاركة فورية عبر expo-sharing |
| ✅15 | **فحوصات الأداء والاختراق واختبار التحمل (F4, F5, F6)** | `scripts/perf-benchmark.ts`, `security-pentest.ts`, `load-test.ts` | p95 = 0.33ms، خطأ 0.00% |
| ✅16 | **حزمة اختبارات التكامل الشاملة للمسارات الذهبية (F1)** | `e2e/*.spec.ts`, `e2e/runner.ts` | 62/62 فحص تكامل ناجح 100% |

---

## أولوية عالية — قبل الإصدار على المتجر

### ع1. نشر وتشغيل الترقيات الخادمية
**الملفات:** `supabase/WEB_EDITOR_UPGRADE_5.sql`, `supabase/functions/push-dispatch/index.ts`
**الخطوات:**
1. شغّل `WEB_EDITOR_UPGRADE_5.sql` كـ Query واحدة في SQL Editor (مشروع مطبَّق عليه 0001–0024).
2. `supabase functions deploy push-dispatch --no-verify-jwt`
3. `supabase secrets set PUSH_DISPATCH_SECRET=$(openssl rand -hex 32)`
4. اجدول استدعاءها كل دقيقة عبر `pg_cron` + `pg_net` (المثال في رأس ملف الدالة).
5. تحقق: `select count(*) from push_outbox where status='pending'` يجب أن يهبط للصفر خلال دقيقتين.

**المخاطر:** لو `pg_cron` غير مفعّل، بلوكا `DO $$` في `0026` يبتلعان الخطأ بهدوء — الجداول تُنشأ لكن التنظيف لا يُجدوَل. تحقق يدويًا بـ `select * from cron.job`.
**التراجع:** `DROP TRIGGER notifications_push_fanout ON public.notifications;` — يوقف الدفع فورًا دون المساس بالإشعارات داخل التطبيق.

### ع2. اختبار الحضور على جهاز حقيقي
**الملفات:** `src/shared/location.ts`, `src/features/attendance/ScannerScreen.tsx`
**الخطوات:** بناء EAS dev-client → اختبر 4 سيناريوهات: داخل النطاق · خارجه · إذن مرفوض · GPS بطيء (>6ث) · ثم مجموعة بـ `geofence_enabled=false` (يجب ألا تُطلب الأذونات إطلاقًا).
**المخاطر:** دقة GPS داخل المباني الخرسانية قد تتجاوز نصف القطر الافتراضي 500م.
**التراجع:** `UPDATE batches SET geofence_enabled=false` — الميزة اختيارية بحكم التصميم.

### ع3. فحص بصري عربي على أندرويد حقيقي
**الملفات:** `src/design/tokens.ts` (ثوابت `uiScale`)
**الخطوات:** 360 / 390 / 430pt × (تكبير خط النظام 100% و140%) — ركّز على `Header`, بطاقات الدوري, `StatBubble`.
**المخاطر:** حدود 0.92–1.08 قد تكون ضيقة على التابلت.
**التراجع:** `uiScale` دالة واحدة — `return 1` يعيد السلوك القديم بالكامل.

---

## أولوية متوسطة

### م1. تصدير تقارير المنظمة (T2 من التحليل التنافسي)
**الملفات:** `src/features/org/AdminScreens.tsx`, `src/shared/export.ts` (البنية جاهزة: `toCsv`/`saveCsv` بـ BOM للعربية)
**لماذا:** أول سؤال يسأله أي مدير مركز، و`get_detailed_course_analytics` منفّذة بالفعل — ينقص زر التصدير فقط. عائد عالٍ/جهد منخفض.
**التراجع:** إزالة الزر — لا تغيير خادمي.

### م2. بطاقة إنجاز قابلة للمشاركة (T4)
**الملفات:** `src/design/celebrations.tsx`, `src/features/gamification/`
**لماذا:** أعمق ميزة عندنا (ستريك/دوري/شارات) لا تخرج من التطبيق إطلاقًا — نمو عضوي مجاني ضائع.
**المخاطر:** خصوصية — لا تُدرج اسم الفرع أو المنظمة افتراضيًا.

### م3. مراجعة استعلامات القراءة
**الملفات:** `src/data/remote.ts`
**الخطوات:** قِس `fetchRemoteDb` على منظمة بـ 500+ متدرب؛ إن تجاوزت 3 ثوانٍ، انقل الجداول الثقيلة إلى RPCs مُرقَّمة (نمط `list_visible_profiles` القائم).
**التراجع:** الدوال منفصلة — يمكن التراجع لجدول واحد في كل مرة.

---

## أولوية منخفضة

### خ1. Open Badges 3.0 (T1) — الربع القادم
أكبر فجوة استراتيجية: الشهادة اليوم تحقُّقها **مركزي**. OB3 يجعلها قابلة للتحقق مستقلًا عن خوادمنا.
**ملاحظة من البحث:** البلوكشين **غير مطلوب** — توقيع تشفيري فقط. أي اقتراح «شهادات بلوكشين» يُرفض.
**الملفات المتوقعة:** migration جديدة (`credential_json` على `certificates`) + Edge Function توقيع + `VerifyScreen`.

### خ2. بصمة جهاز للحضور (T3) — محجوب
يقفل buddy-punching، لكن **يُمنع البدء فيه قبل نشر سياسة خصوصية**. IMEI مستحيل على iOS — البديل معرّف مثبَّت في `expo-secure-store`.

### خ3. توحيد التوثيق
`AUDIT_COMPREHENSIVE_AR.md` و`PRODUCTION_AUDIT_AR.md` و`CODE_REVIEW_2026-08-28.md` فيها تداخل. ادمج التاريخي في أرشيف واحد وأبقِ ملفًا حيًّا واحدًا لكل محور.

---

## مرفوض صراحةً (قرار موثّق)

| البند | السبب |
|---|---|
| استضافة محتوى / SCORM | يحوّل مسار إلى LMS ضعيف أمام 15 منافسًا أقوى. مسار يدير **الحضور والناس**. |
| فوترة ومدفوعات | نطاق Arlo، جهد هائل، خارج التموضع. |
| تخزين إحداثيات كل مسح | نتنازل عن أقوى ميزة خصوصية عندنا مقابل لا شيء. |
| شهادات بلوكشين | ضجيج تسويقي؛ OB3 يحل المشكلة بتوقيع بسيط. |
| `FlatList` استباقيًا | لا قائمة تتجاوز حدود منظمة واحدة — تعقيد بلا قياس. يُعاد النظر عند أول jank مقيس. |
