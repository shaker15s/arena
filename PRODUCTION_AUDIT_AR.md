# تقرير تدقيق الجاهزية الإنتاجية — مسار 3.2

**تاريخ المراجعة:** 23 أغسطس 2026  
**النطاق:** تطبيق Expo/React Native/Web، طبقة البيانات، Supabase/Postgres، RLS، OAuth، QR، الرسائل، الشهادات، الجيميفيكيشن، البناء والنشر.

## 1. الخلاصة التنفيذية

لم تكن المشكلة مجموعة أزرار منفصلة فقط؛ كان المشروع يحتوي على نموذج نطاق محلي جيد للعرض والاختبارات، لكن عدة مسارات إنتاجية كانت تبني تغييرات متعددة في العميل ثم تحاول مزامنتها جدولًا بعد جدول. هذا يسمح بالكتابة الجزئية، ويتعارض مع RLS الصارمة، ولا يتحمل السباقات. كذلك كانت سلسلة migrations متناقضة، والتحقق العام من الشهادة والانضمام عبر QR يعتمدان على بيانات محلية أو روابط غير موصولة.

أُعيدت الحدود الأمنية بحيث تكون العمليات الحساسة معاملات Postgres ذرية ومرتبطة بهوية `auth.uid()`، بينما بقي المحرك المحلي نموذجًا حتميًا للاختبارات والعرض فقط. أزيلت بيانات المؤسسة التجريبية، ووُحّدت سلسلة قاعدة البيانات، وأُضيفت مهام تلقائية وCI. النتيجة أقرب كثيرًا إلى منتج قابل للإطلاق، لكن اعتماد الإنتاج النهائي ما زال مشروطًا بمرور `supabase db reset` في CI على Postgres حقيقي واختبارات EAS على أجهزة حقيقية.

## 2. حالة المتطلبات المبلّغ عنها

| المتطلب | الحالة | التنفيذ |
|---|---|---|
| حفظ تغييرات دور المستخدم وحالته | منفذ | `admin_update_user_access` يقفل الصف، يحمي آخر أدمن ومنع تعطيل الذات، ويدوّن التغيير. |
| شاشة تفاصيل حقيقية لكل كورس | منفذ | شاشة إدارة تعرض المجموعات، كل مجموعة منفصلة، الطلاب، الجلسات، الحضور، السعة، المدرب، الفرع والجدول. |
| عدم إظهار كورس مكتمل بلا بيانات صحيحة | منفذ | `isBatchComplete` يتطلب مجموعة مكتملة، طالبًا فعليًا، العدد المتوقع من الجلسات، وإغلاقها كلها؛ إصدار الشهادة والتقييم يطبقان نفس الشرط خادميًا. |
| رؤية الأدمن والمتطوع للتفاصيل والمسجلين | منفذ | الوصول مقيد بدور المدير أو المدرب المعيّن، مع ملفات اتصال للمجال المسموح فقط. |
| تسجيل الطالب في الكورس | منفذ | `join_batch` يقفل صف المجموعة ويحسب المقعد/الانتظار دون overbooking. |
| الانضمام برمز/QR | منفذ | رمز عشوائي خادمي، `join_batch_by_code`، شاشة تأكيد، وروابط `/join?code=...` حقيقية. |
| طلب كورس من متطوع | منفذ | طلب محفوظ موجّه إلى متطوع نشط، مع inbox وحالة ورد وإشعار. |
| طلب ترقية من الأدمن | منفذ | يصل للأدمن النشط، ولا يستطيع المشرف أو المتطوع مراجعته. |
| بث الأدمن للجميع | منفذ | إدخال جماعي ذري مع عدد وصول حقيقي للمستخدمين النشطين وتدقيق. |
| QR حضور حقيقي | منفذ | كاميرا حقيقية، توقيع خادمي دوّار كل 25 ثانية، قبول النافذة الحالية/السابقة فقط، حد زمني، وكود احتياطي. |
| إزالة المحاكاة | منفذ في المسارات النشطة | أزيلت بيانات المؤسسة المزروعة، الوصول البيومتري الوهمي، الوصولات المصطنعة، التحقق المحلي الوهمي، وQR الانضمام غير الموصول. |

## 3. الإصلاحات المعمارية والأمنية

### 3.1 حدود المعاملات

تحولت العمليات التالية إلى RPCs `SECURITY DEFINER` ذات `search_path` ثابت ومنح صريح للمستخدم الموثق:

- إكمال وتعديل الملف الشخصي.
- تغيير الدور والحالة.
- التسجيل بالمجموعة أو رمز الانضمام.
- بدء الجلسة، توليد QR، تسجيل الحضور، الحضور اليدوي، والإغلاق.
- تقديم ومراجعة الأعذار.
- تقييم الكورس ومنح نقاط التقييم مرة واحدة.
- الكودوس بكوتا مقفلة و`idempotency_key` حقيقية.
- إنشاء الفرع واللجنة والكورس.
- نشر المجموعة وكل جلساتها في معاملة واحدة مع كشف تعارض المدرب خادميًا.
- معالج إنشاء المؤسسة الكامل في معاملة واحدة.
- إصدار الشهادات ونقاط الإتمام والإشعارات.
- قواعد الجيميفيكيشن وتفعيل الشارات.
- طلبات الدعم/الكورس/الترقية والبث.
- التحقق العام الآمن من الشهادة.

### 3.2 RLS وتقليل البيانات

- الكتابات الحساسة RPC-only؛ لا يستطيع العميل تزوير حضور أو نقاط أو شهادة أو دور أو مجموعة.
- دليل الملفات يحجب البريد والهاتف ومعرّف Auth عن المجتمع، ويكشف الاتصال فقط للذات، المدير، أو مدرب الطالب.
- الحساب المعطّل يحصل على صفه فقط ليظهر حاجز الإيقاف، ولا يستطيع قراءة مجال التطبيق أو تنفيذ RPCs النشطة.
- `qr_seed` غير متاح عبر `SELECT`، ويُمسح عند إغلاق الجلسة.
- التحقق العام يعيد فقط: السيريال، تاريخ الإصدار، اسم الطالب، الكورس والفرع.
- الملاحظات الخاصة لا يقرأها أو يكتبها إلا صاحب الملاحظة نفسه.
- الملف الشخصي لم يعد قابلًا للتعديل المباشر؛ بيانات الملف ومسار الصلاحيات منفصلان.

### 3.3 سلامة البيانات والسباقات

أضيفت قيود وفهارس فريدة للجلسة داخل المجموعة، عذر الطالب للجلسة، شهادة الطالب للمجموعة، أسماء اللجان داخل الفرع، وتقييمات المجالات. التسجيل يقفل المجموعة، والكودوس يقفل الكوتا، والمراجعات والإصدار يقفلان/يتعاملان مع التعارض بصورة idempotent.

### 3.4 قاعدة البيانات القانونية

- المصدر القانوني الوحيد: `supabase/migrations`.
- `0001` مخطط كامل بلا فروع أو كورسات وهمية.
- أزيل `supabase/001_complete_schema.sql` المتناقض.
- أزيلت RPCs القديمة التي تقبل هوية مستخدم يرسلها العميل.
- نُقلت الأتمتة المتوافقة إلى `0006_automation_jobs.sql` بعد اكتمال مخطط الحماية.
- قواعد اللعبة والشارات وحدها بيانات مرجعية؛ شارة الموسم معطلة إلى أن يوجد نطاق Season حقيقي.
- أول زائر لا يصبح أدمن؛ bootstrap الأدمن يتطلب App Metadata خادمية أو إجراء SQL مضبوطًا وموثقًا.

## 4. الأتمتة والجيميفيكيشن

المهام المجدولة idempotent وتشمل:

1. إغلاق الجلسات الحية المنسية بعد المدة وساعة سماح، وتسجيل الغياب.
2. تسوية أسبوع الستريك بتوقيت القاهرة وبداية الأحد، وانتظار الأعذار المعلقة.
3. إغلاق دوري الأسبوع السابق وتحديث الفئة للمجموعات الكافية فقط.
4. بونص الشهر للحضور الكامل ومنح «نجم الشهر».
5. تذكير الطلاب والمدرب بالجلسة قبلها، مع `dedupe_key`.

تقييم الشارات الخادمي يغطي البداية، المواظبة، الحضور المبكر، الستريك، الشهر الكامل، الشهادات والتقييمات؛ وتمنح مهام الدوري شارات المتصدر والصاعد.

## 5. تجربة المستخدم والتكاملات الحقيقية

- الكاميرا تستخدم `expo-camera` بدل إدخال مصطنع.
- CSV على الويب ملف تنزيل، وعلى native ملف cache حقيقي ثم Share Sheet.
- الشهادة تُنسخ بالحافظة وتُنشأ PDF للطباعة/التنزيل/المشاركة.
- رابط التحقق مصدره `EXPO_PUBLIC_APP_URL`، مع مساري deep link للانضمام والتحقق.
- التحقق لا يعتمد على cache التطبيق أو تأخير شكلي؛ يستدعي RPC عامة محدودة البيانات.
- شاشتا إدارة الكورس والطلبات أضيفتا إلى قاموسي العربية والإنجليزية.
- الحساب المعطّل يدخل حاجزًا لا يحتوي إلا على تسجيل الخروج.
- القراءة من PostgREST أصبحت paginated بدل حد 5000 الصامت؛ سجل العمليات يعرض آخر 500 حدث بترتيب حديث.

## 6. ما حُذف أو أوقف عمدًا

- شاشات Dashboard/Details المكررة وغير المركبة.
- دوال Supabase عميلة قديمة غير مستخدمة للتقييم والحضور وكانت تتجاوز العقود الجديدة.
- بيانات فروع RTC وكورسات العينة من migration الإنتاج.
- التحكم البيومتري الوهمي ومفاتيح إشعارات غير موصولة.
- ادعاء تنزيل PNG للشهادة.
- مرفق عذر ظاهر بلا منتقي ملفات أو رفع حقيقي.
- شارة الموسم النشطة، لأن المشروع لا يملك بعد جدول موسم أو تسوية موسم قابلة للتدقيق.

## 7. نتائج التحقق الحالية

- TypeScript: ناجح بلا أخطاء.
- اختبارات المحرك: **51/51**، وتشمل الآن رفض الإكمال الوهمي بلا طلاب/جلسات.
- تطابق i18n: **579/579**.
- Web export: ناجح.
- `git diff --check`: ناجح.
- تحليل SQL بـ`pglast`: نجحت ملفات migrations وseed نحويًا.
- `npm audit --omit=dev --audit-level=high`: **0 high / 0 critical**؛ بقيت 11 moderate انتقالية من سلسلة Expo tooling/`uuid`، والإصلاح القسري يقترح downgrade مكسورًا إلى Expo 46 لذلك لم يُطبّق.
- Expo Doctor السابق: 19/21؛ الفشلان كانا طلبات API/TLS خارجية، ولم يعد هناك تكرار محلي لوحدات Expo.

## 8. المخاطر المتبقية قبل زر الإنتاج

### مانعة لاعتماد الإنتاج النهائي

1. **اختبار قاعدة حقيقية:** `pglast` يفحص النحو فقط. أضيف job في CI يشغّل Supabase محليًا ثم `db reset` من قاعدة فارغة. يجب أن ينجح هذا job قبل الدمج.
2. **اختبارات RLS تكاملية:** يجب إنشاء مستخدم لكل دور على staging وتجربة السماح/المنع والسباقات، وليس الاعتماد على اختبارات المحرك.
3. **اختبار أجهزة EAS:** الكاميرا، callback جوجل، الصور، PDF، الطباعة، CSV والمشاركة تحتاج development build على iOS وAndroid.
4. **إعداد Bootstrap/OAuth/Domain:** لا إطلاق قبل إنشاء الأدمن المقصود وضبط redirects و`EXPO_PUBLIC_APP_URL`.

### تشغيلية

- راقب `cron.job_run_details`؛ فشل `pg_cron` يعطل التسويات والتذكيرات.
- القراءة المحمولة ترفض صراحة تجاوز 100 ألف صف بدل تقديم بيانات ناقصة. قبل الوصول لهذا الحجم يجب تحويل دفتر النقاط وشاشات الإدارة إلى views مجمعة وpagination شاشة-بشاشة.
- سجل العمليات في العميل يعرض أحدث 500 فقط؛ السجل الكامل يبقى في Postgres.
- لا توجد حاليًا منظومة crash reporting/observability خارج Supabase logs؛ يجب اختيار خدمة ومراعاة الخصوصية قبل الإطلاق العام.

## 9. خطة النشر الموصى بها

1. افتح PR واترك jobي التطبيق وقاعدة البيانات يمران.
2. أنشئ مشروع Supabase staging جديدًا من سلسلة migrations الحالية.
3. أنشئ أدمن bootstrap بالطريقة الموثقة في `SUPABASE_SETUP.md`.
4. اضبط Google OAuth والدومين ومتغيرات كل بيئة.
5. أنشئ مؤسسة فعلية من المعالج، ثم نفّذ سيناريو كامل: طالب → تسجيل → QR → إغلاق → عذر → تقييم → شهادة → تحقق عام.
6. نفّذ اختبارات سباق: آخر مقعد، QR مكرر، إغلاق مرتين، كودوس مكرر، إصدار شهادات متزامن.
7. نفّذ EAS development builds واختبارات الجهاز.
8. جهّز النسخ الاحتياطية والتنبيهات وخطة rollback، ثم انشر production ببيانات المؤسسة الحقيقية فقط.

## 10. قرار الجاهزية

**جاهز للانتقال إلى staging المنضبط، وليس للإطلاق الأعمى مباشرة.**  
أُغلقت العيوب الوظيفية والأمنية الأساسية في الكود، لكن ختم production يتطلب نجاح قاعدة Postgres الحقيقية وRLS وEAS، لأنها حدود لا يستطيع web export أو المحرك المحلي إثباتها.
