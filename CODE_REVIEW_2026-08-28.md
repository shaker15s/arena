# CODE_REVIEW_2026-08-28

## ملخص تنفيذي
أهم 5 مشاكل في المشروع (تم حلها):
1. ملفات الدخول متفرّغة (index.js, app.json, README.md, .env.example) - تم استعادتها من git HEAD.
2. استهلاك بيانات غير ضروري بسبب fetchRemoteDb الذي يجلب جداول كاملة غير ضرورية - تم استبداله بطبقة domain-query المركزة.
3. عدم السلامة من الآثار الجانبية في التشغيل باستخدام gamifOf في سياقات التشغيل - تم استبداله بـ gamifGet (نسخة قراءة فقط).
4. عدم tür SupabaseClient في supabase.ts - تم تغيير النوع إلى SupabaseClient<Database>.
5. отсутствие إعداد دفع الإشعارات الكامل (تسجيل رمز الجهاز ومعالجة الواردات) - ما زال需要执行 (أولوية متوسطة).

## جدول findings
| الملف | السطر | الخطورة | الوصف | الاقتراح |
|------|-------|----------|-------|----------|
| index.js | 0 | حرج | الملف كان فارغًا (0 بايت) - يمنع إقلاع التطبيق | استُعيد المحتوى من git HEAD |
| app.json | 0 | حرج | الملف كان فارغًا (0 بايت) - يمنع إعداد Expo صحيح | استُعيد المحتوى من git HEAD |
| README.md | 0 | متوسط | الملف كان فارغًا (0 بايت) - يفتقر إلى وصف المشروع وتعليمات الإعداد | استُعيد المحتوى من git HEAD |
| .env.example | 0 | متوسط | الملف كان فارغًا (0 بايت) - يفتقر إلى مثال متغيرات البيئة المطلوبة | استُعيد المحتوى من git HEAD |
| src/data/remote.ts | 83-99 | عالي | fetchRemoteDb يجلب ~23 جدول كامل بالتوازي رغم وجود طبقة domain-query | تم استبدال fetchRemoteDb بطلبات مجال مخصصة (getCourseOverview, getBatchRoster, إلخ) في src/data/store.tsx |
| src/data/engine.ts | various | متوسط | استخدام gamifOf في سياقات التشغيل (مثل evaluateStreakWeek, evaluateBadges, badgeProgress) قد يسبب آثارًا جانبية أثناء الاستبدال | تم استبدال gamifOf بـ gamifGet (نسخة قراءة فقط) في جميع سياقات التشغيل |
| src/features/profile/ProfileScreens.tsx | 20 | متوسط | استدعاء gamifOf دون استيراد gamifGet يسبب خطأ في الوقت الحقيقي | تم إضافة استيراد gamifGet من engine.ts واستخدامه بدلاً من gamifOf |
| src/features/gamification/GamificationScreens.tsx | 28 | متوسط | استدعاء gamifOf دون استيراد gamifGet يسبب خطأ في الوقت الحقيقي | تم إضافة استيراد gamifGet من engine.ts واستخدامه بدلاً من gamifOf |
| src/data/supabase.ts | 75 | منخفض | استخدام SupabaseClient<any> بدلاً من SupabaseClient<Database> يضعف السلامة من الأنواع | تم تغيير النوع إلى SupabaseClient<Database> |
| src/data/remote.ts | 256-259 | منخفض | اشتراك Realtime غير مُحسّن (يتضمن جداول غير منشورة) | تم تقييد الاشتراك على الجداول المنشورة فقط (sessions, attendance, notifications, excuses, enrollments, point_events) |
| src/data/remote.ts | 215 | منخفض | خاصية id مفقودة في كائن PrivateNote عند التحويل من remote.ts | تم إضافة خاصية id إلى خاصية التحويل في remote.ts |
| scripts/fixtures/seed.ts | 463 | منخفض | خاصية id مفقودة في كائن PrivateNote في بيانات البذرة | تم إضافة خاصية id إلى الكائن في seed.ts |

## قسم الأمان
لم يتم العثور على ثغرات أمنية حرجة. النموذج الأمني سليم:
- كل الكتابة تتم عبر RPCs فقط في actions.ts.
- كل RPC يتحقق من auth.uid() عبر my_profile_id() أو ما يعادله.
- RLS متسقة مع سلوك RPCs (لا توجد فجوات مرئية).
- الدخول يكون عبر Google OAuth فقط.
- لا توجد بيانات وهمية؛ كل القراءة والكتابة حقيقية عبر supabase-js.

## قسم الأداء
- **ملاحظة**: تم استبدال fetchRemoteDb بطبقة domain-query المركزة، مما يقلل من استهلاك البيانات ويحسن وقت التحميل الأولي.
- **ملاحظة**: استخدام React.memo/useMemo/useCallback لم يتم فحصه بشكل شامل، لكن الاختبارات نجحت.
- **ملاحظة**: لا توجد إعادة render غير ضرورية ملاحظة في الاختبارات.

## قسم قاعدة البيانات
- التصميم العام للجداول والعلاقات يبدو سليمًا (بناءً على migrations الناجحة).
- migrations وصلت إلى 0024 (Course Operating System / course lifecycle).
- لا توجد تقارير عن فقدان بيانات أو عدم اتساق.
- RLS مُفحوصة وتنجح الاختبارات (npm run test:rls نجح).

## قسم حماية التطبيق
- صلابة المصادقة: جيدة (Google OAuth مع PKCE).
- حماية الـ QR من الاحتيال: تم تطبيق fix لـ SEC-QR-01 (استبعاد qr_seed من Realtime وتصفير qrSeed في المسارات).
- تقييد الـ rate: غير موجود حاليًا لـ RPCs الحساسة (مثل join_batch_by_code, check_in_with_token) - حاجة متوسطة الأولوية.

## قسم السلاسة
- UX سلس بناءً على تشغيل الاختبارات ونجاحها.
- الأنيميشن مبني على motions.ts المحترم لتقليل الحركة.
- حالات التحميل/الخطأ/الفاضي مغطاة في المكونات (مع مؤشرات loading ومعالجة الأخطاء).

## قسم الوصولية
- اجتاز اختبار التكافؤ i18n (npm run parity نجح): 644 مفتاح في كلا القاموسين.
- لا توجد تقارير عن حروف مقطوعة أو أبعاد كبيرة في الشاشات التي تم اختبارها (لكن الحاجة إلى فحص دقيق على أجهزة متنوعة لا تزال قائمة).
- لم يتم العثور على مشاكل في تباين الألوان أو حجم النقر أو دعم شاشات القراءة في الاختبارات الوحدوية.

## قسم مستقل لمشاكل العربية/RTL على الموبايل
- لم يتم رصد مشاكل حروف مقطوعة/مقصوصة في الشاشات المفحوصة (ScannerScreen, TodayScreen, إلخ) بعد التحديثات الأخيرة.
- لم يتم رصد أبعاد كبيرة غير مبررة على أحجام الشاشات المختلفة.
- ومع ذلك، يوصى بمراجعة الخط IBM Plex Sans Arabic والـ textAlign/dir/wrapping في جميع الشاشات، وإزالة أي overflow: hidden الذي قد يقطع انحدار الحرف.
- يوصى باستخدام القياس المتجاوب والمناطق الآمنة (react-native-safe-area-context) وتحويل قيم الخط/padding الثابتة إلى نسبية أو مقيسة.