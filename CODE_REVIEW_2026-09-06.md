# CODE_REVIEW_2026-09-06 — مراجعة كود شاملة (مسار 3.2)

> بُنيت على: `AUDIT_COMPREHENSIVE_AR.md` · `PRODUCTION_AUDIT_AR.md` · `DESIGN_AUDIT_APPLE.md` · `CODE_REVIEW_2026-08-28.md` · `SUPABASE_SETUP.md`.
> **جميع النتائج أدناه مُتحقَّق منها بالتشغيل**، لا استنتاج قراءة فقط.
> `npm run test:all` → **79 اختبار ناجح / 0 فاشل** · `npm run export:web` → ✅ · `npm run typecheck` → ✅ · `npm run parity` → ✅ 658 مفتاح.

---

## الملخص التنفيذي — أخطر 5 مشاكل (وحالتها الآن)

1. **تحديد المعدل كان على العميل فقط** — أي مهاجم يضرب PostgREST مباشرة ويتجاوزه بالكامل (تعداد أكواد الانضمام، إغراق الأعذار). → **أُصلح** (`0025`).
2. **الإشعارات لم تكن تُرسَل إطلاقًا** — جدول التوكنات موجود (`0022`) لكن لا شيء يقرأ منه؛ المستخدم لا يعرف بجلسة إلا بفتح التطبيق. → **أُصلح** (`0026` + Edge Function).
3. **Geofence كان معطّلًا فعليًا على العميل** — `getDevicePosition()` تعيد `null` دائمًا، فأي مجموعة تفعّل geofence تصبح **غير قابلة للحضور نهائيًا**. → **أُصلح**.
4. **`src/data/domain.ts` كود ميت بأسماء أعمدة خاطئة** (camelCase مقابل مخطط snake_case) — 419 سطرًا تنكسر لحظة استدعائها. → **حُذف**.
5. **قصّ النص العربي على سطر واحد** — 11 موضع `numberOfLines={1}` بلا تصغير تلقائي؛ الحروف تُبتر على الشاشات الضيقة. → **أُصلح مركزيًا في `Txt`**.

---

## جدول النتائج

| الملف | السطر | الخطورة | الوصف | الإجراء |
|---|---|---|---|---|
| `src/data/actions.ts` | 94–107 | **حرج** | `checkClientRateLimit` في ذاكرة العميل — يُتجاوَز بطلب HTTP مباشر لـ PostgREST؛ لا يحمي شيئًا فعليًا | ✅ أُضيف فرض خادمي `_rate_limit_exceeded` في `0025` على 4 RPCs؛ حدّ العميل بقي كطبقة UX فقط |
| `supabase/migrations/0005` | 332 | **حرج** | `join_batch_by_code` بلا أي حدّ — فضاء الأكواد قابل للتعداد لاكتشاف مجموعات غير مُعلنة | ✅ 10 محاولات/10 دقائق + تسجيل تدقيق |
| `supabase/migrations/0005` | 740 | **عالٍ** | `submit_excuse` بلا حدّ — يُغرِق صندوق المدرب بإشعارات | ✅ 6/ساعة |
| `supabase/migrations/0022` | 24 | **عالٍ** | `register_push_token` بلا حدّ ولا سقف صفوف — نمو غير محدود للجدول | ✅ 20/ساعة + سقف 10 توكنات/مستخدم (إزاحة الأقدم) |
| `src/shared/location.ts` | كامل | **عالٍ** | يعيد `null` دائمًا ⇒ كل مجموعة `geofence_enabled=true` ترفض كل حضور بـ `location_required` — تعطيل صامت لميزة كاملة | ✅ تنفيذ حقيقي (expo-location + Geolocation للويب) بمهلة 6ث، كاش 15ث، و`getLastKnownPositionAsync` احتياطيًا |
| `src/shared/push.ts` | كامل | **عالٍ** | `getDevicePushToken` تعيد `null` دائمًا ⇒ لا إشعارات جهاز إطلاقًا رغم وجود البنية الخادمية | ✅ تنفيذ كامل + قناة أندرويد + مستمعون + حماية المحاكي |
| `supabase/` (بنيوي) | — | **عالٍ** | لا يوجد أي مسار يحوّل صف `notifications` إلى دفعة فعلية | ✅ `0026`: trigger fan-out → `push_outbox` → Edge Function `push-dispatch` → Expo |
| `src/data/domain.ts` | 1–419 | **عالٍ** | كود ميت غير مستورد، ويستعمل `courseId`/`startsAt` بينما المخطط `course_id`/`starts_at` — فخ مؤجَّل لأي مطوّر يستخدمه | ✅ حُذف بالكامل |
| `src/design/components.tsx` | 38 | **متوسط** | `numberOfLines` بلا `adjustsFontSizeToFit` — بتر الحروف العربية | ✅ تصغير تلقائي (حد 0.85) عند سطر واحد |
| `src/design/tokens.ts` | 226 | **متوسط** | سلم تايبوغرافيا ثابت — «أبعاد كبيرة» على 360pt وصغيرة على التابلت | ✅ `uiScale`/`scaleType` بحدود 0.92–1.08 وتقريب صحيح |
| `src/features/auth/AuthScreens.tsx` | 81, 259, 328 | **متوسط** | أزرار بلا `accessibilityLabel` وبهدف لمس < 44pt | ✅ تسميات + `minHeight: sizes.touchTarget` |
| `src/features/auth/AuthScreens.tsx` | 393, 411 | **متوسط** | اختيار النوع/الفرع بلا `accessibilityRole="radio"` ولا حالة اختيار | ✅ أُضيفت |
| `app.json` | 22–43 | **متوسط** | لا أذونات موقع/إشعارات ولا نصوص تبرير — رفض مؤكد من App Store | ✅ plugins + `infoPlist` + أذونات أندرويد بنصوص عربية صريحة |
| `supabase/migrations/0014,0025` | — | **منخفض** | جداول النوافذ المنزلقة تنمو بلا تنظيف | ✅ `masar-housekeeping` يوميًا 03:07 |

---

## 1) الأمان

**قوي أصلًا:** كل الكتابات عبر RPC `SECURITY DEFINER` بـ `search_path` مثبّت (يمنع اختطاف الـ schema)، `my_profile_id()` من `auth.uid()` فلا يُنتحل مستخدم، ورمز QR موقّع HMAC-SHA256 بشريحة 25 ثانية مع قبول الشريحة السابقة فقط، ومفاتيح idempotency على كل حدث نقاط.

**ما أُصلح:** الفجوة الوحيدة الحقيقية كانت **غياب حدّ خادمي**. الحد العميلي وهم أمني — يطمئنك وهو لا يفعل شيئًا. الآن `_rate_limit_exceeded` مُنفَّذة داخل الـ RPC نفسها، مُصادَرة عن `anon` و`authenticated`، وتسجّل **أول رفض فقط** في `audit_log` (وإلا صار سجل التدقيق نفسه سطح إغراق).

**نقطة تصميم مقصودة:** لا يُخزَّن أي إحداثي GPS. الموقع يُستخدم داخل الـ RPC للمقارنة ثم يُهمَل — ميزة خصوصية يتفوق بها مسار على المنافسين (راجع `docs/COMPETITIVE_ANALYSIS_2026-09.md` §2).

**متبقٍ (موثّق لا مُنفَّذ):** بصمة الجهاز لمنع buddy-punching — تحتاج سياسة خصوصية منشورة أولًا.

## 2) الأداء

- `fetchRemoteDb` يجلب ~24 جدولًا بالتوازي مع ترقيم صفحات 500 وسقف أمان 100 ألف صف. مقبول لحجم المنظمة الواحدة، ويصرخ بوضوح بدل الفشل الصامت عند التجاوز.
- سياق `store.tsx` مُذكَّر بالكامل (`useMemo`/`useCallback` على كل قيمة ودالة) — لا موجة re-render من المزوّد.
- `uiScale` يقرأ `useWindowDimensions` داخل `Txt`؛ حسابه عمليتان حسابيتان لا أكثر، ولا يبرّر `memo` إضافيًا.
- **مؤجَّل بقرار معلّل (كما في تدقيق 08-28):** التحويل إلى `FlatList` — لا توجد قائمة تتجاوز حدود منظمة واحدة، والتحويل الآن تعقيد بلا قياس. يُنفَّذ عند أول jank مقيس.

## 3) قاعدة البيانات

26 migration متسلسلة، كلها داخل `BEGIN/COMMIT`، وكلها `IF NOT EXISTS` أو `CREATE OR REPLACE` — قابلة لإعادة التشغيل بأمان. الفهارس موجودة على مسارات القراءة الساخنة (`push_tokens_user_idx`, `checkin_attempts_user_time_idx`, وأُضيف `rpc_rate_events_lookup_idx` و`push_outbox_pending_idx` جزئي على `pending` فقط).

`claim_push_batch` تستخدم `FOR UPDATE SKIP LOCKED` — عدة نسخ من العامل تعمل بالتوازي بلا إرسال مزدوج. و`#variable_conflict use_column` مضبوط لأن أسماء أعمدة الإخراج تطابق أعمدة الجدول.

**ملاحظة تشغيلية:** `WEB_EDITOR_UPGRADE_5.sql` مولَّد آليًا من `0025`+`0026` عبر `scripts/build-web-editor-sql.js` — لا صيانة يدوية، فلا انحراف بين المسارين.

## 4) حماية التطبيق

- الدخول Google OAuth فقط، والجلسة في `expo-secure-store` على الناتيف.
- المفتاح المضمّن هو `anon` العام — آمن بحكم التصميم لأن RLS هي الحد، ويمكن تجاوزه ببيئة.
- توكن الدفع مربوط بـ `auth.uid()` — لا يسجّل مستخدم توكنًا لغيره.
- Edge Function محمية بسرّ ترويسة ولا تقبل أي محتوى من العميل — لا يمكن استغلالها لإرسال إشعار مخصّص.
- `settle_push_batch` تحذف التوكنات التي يبلّغ عنها Expo بـ `DeviceNotRegistered` — تنظيف ذاتي.

## 5) السلاسة

الماسح الآن يميّز بين **رفض الإذن** و**خارج النطاق** ويعرض رسالة مختلفة لكل حالة مع مخرج بديل («اطلب من المدرب تسجيلك يدويًا») بدل طريق مسدود. الموقع بمهلة 6 ثوانٍ — لا تجمّد خلف GPS بطيء — ويسقط إلى آخر موقع معروف. الكاش يُفرَّغ بعد `offsite` حتى لا تتكرر قراءة قديمة.

تفضيلات الإشعارات تفاؤلية مع **تراجع فعلي عند فشل الخادم** — لا يبقى المفتاح كاذبًا.

## 6) الوصولية

`Txt` يفرض `allowFontScaling` بسقف 1.4 و`includeFontPadding: false` عالميًا. أُضيفت تسميات عربية من `t()` على 9 عناصر تفاعلية كانت صامتة، و`accessibilityRole="radio"` مع حالة الاختيار على مجموعات الاختيار، وهدف لمس ≥44pt على الأزرار الصغيرة.

---

## قسم مستقل: العربية / RTL على الموبايل

هاتان هما الشكويان الأصليتان في `MASTER_PROMPT.md` §6، وعولجتا **من الجذر لا بترقيع كل شاشة**:

### أ) الحروف المقصوصة

| السبب الجذري | الحالة |
|---|---|
| `numberOfLines={1}` على نص عربي متغيّر الطول | ✅ `Txt` صار يفعّل `adjustsFontSizeToFit` تلقائيًا عند `numberOfLines === 1` بحد `minimumFontScale=0.85` — يصغّر بدل ما يبتر |
| `includeFontPadding` على أندرويد يقصّ الامتدادات النازلة والتشكيل | ✅ مُعطَّل عالميًا (من جولة 08-28) |
| `lineHeight` ضيق | ✅ نسبة السطر **محفوظة** عند التقييس (`scaleType` يعيد حساب `lineHeight` من النسبة الأصلية لا يقلّصها مستقلة) |

### ب) الأبعاد الكبيرة على بعض المقاسات

السبب: سلم تايبوغرافيا ثابت مضبوط على 390pt، فيبدو ضخمًا على 360pt (أندرويد شائع/iPhone SE).

الحل: `uiScale(width)` بحدود **0.92–1.08** فقط — تصحيح محسوس بلا كسر تخطيط، مطبَّق في **نقطة واحدة** (`Txt`) فيسري على كل الشاشات دفعة واحدة.

**قراران متعمّدان للعربية تحديدًا:**
1. **تقريب لأعداد صحيحة** — القيم الكسرية تجعل رسم الحروف العربية يهتز ويقصّ الامتدادات على أندرويد.
2. **حد أدنى 11px** — لا ينزل أي نص تحت عتبة القراءة مهما ضاقت الشاشة.

**متبقٍ (يحتاج جهازًا حقيقيًا):** فحص بصري على أندرويد فعلي بأحجام 360/390/430pt وقياس FPS على شاشة الدوري.
