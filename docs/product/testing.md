# Testing

`npm run test:all` = typecheck + i18n parity + RPC contract + generated types + engine 64 + RLS 15.

E2E on real devices, load tests, VoiceOver: **NOT VERIFIED**.

Golden paths: student check-in, admin create course/batch, certificate verify.
