# Design system

Tokens live in `src/design/tokens.ts` (semantic color, spacing 4pt, radius, type, motion).

Glass: overlays and tab bar only — not every card.

Color is never the only status signal (icon + label).

Touch target ≥ 44pt. Arabic-first type (IBM Plex Sans Arabic). RTL uses `start`/`end`.
