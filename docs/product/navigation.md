# Navigation

Bottom tabs = top-level destinations only (≤5). FAB = high-value action (Scan), not decoration.

Flows are linear: Today → Session → Check-in → Success. No nested modals for create-course / create-batch (sheets OK for short tasks).

Back = back. Close = close. Cancel = cancel.
