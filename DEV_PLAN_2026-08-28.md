# DEV_PLAN_2026-08-28

## أولوية عالية (≥5 بنود)

### مكتمل:
1. **استعادة ملفات الدخول المتفرّغة** — الملفات: index.js, app.json, README.md, .env.example — الخطوات: استُعيد المحتوى من git HEAD — المخاطر: عدم توافق المحتوى المستعادة مع التعديلات المحلية (لكنها uncommitted 0 بايت، لذا لا يوجد تعارض). — التراجع: الملفات已经是已经 uncommitted، لذا يمكن تركها فارغة أو استعادتها من نسخة احتياطية إذا لزم الأمر (لكننا نستعيد من HEAD، وهو المصدر الصحيح).
2. **استبدال fetchRemoteDb بطبقة domain-query** — الملفات: src/data/store.tsx, src/data/domain.ts (تم إنشاؤه), src/data/engine.ts (للاستخدام الآمن) — الخطوات: 
   1. إنشاء ملف `src/data/domain.ts` يحتوي على وظائف استعلام مFocused:
      - `getCourseOverview(courseId)`: يعيد الدورة مع الحقول الأساسية فقط
      - `getBatchRoster(batchId)`: يعيد المستخدمين المسجلين مع بيانات الملف الشخصي للدفعة
      - `getBatchSessions(batchId)`: يعيد جلسات الدورة مع إحصائيات الحضور
      - `getSessionRoster(sessionId)`: يعيد الحضور للجلسة مع معلومات الحالة
      - `getMyGamificationSafe(userId)`: يعيد بيانات الجيميفكيشن باستخدام `gamifGet` (نسخة آمنة للقراءة فقط)
      - `getActiveBatchesForUser(userId)`: يعيد الدفعات النشطة للمستخدم مع بيانات الدورة/dفعة
      - `getUpcomingSessions(userId)`: يعيد الجلسات القادمة للمستخدم مع معلومات الدورة
   2. تعديل `src/data/store.tsx` في دالة `refresh()` لاستبدال استدعاء `fetchRemoteDb` بالاتصال بهذه الوظائف المجالية وتجميع كائن `Db` متوقع المتوافق مع `engine.ts`.
   3. التأكد من أن الاستعلامات محسّنة لجلب البيانات المرتبطة الضرورية فقط وليس جداول كاملة.
   4. تشغيل أوامر التحقق بعد التنفيذ. — المخاطر: عدم توافق شكل الكائن `Db` الناتج مع توقعات `engine.ts` causando أخطاء في الوقت الحقيقي. — التراجع: الاحتفاظ بنسخة احتياطية من `src/data/store.tsx` قبل التعديل والعودة إليها إذا لزم الأمر.

### المتبقية (الأولوية العالية):
3. **تنفيذ عميل-side فعال لجيوफ़نس (تمرير الإحداثيات للخادم)** — الملفات: src/features/attendance/ScannerScreen.tsx, src/data/actions.ts (ربما يحتاج تعديل RPC) — الخطوات:
   1. تعديل `ScannerScreen.tsx` لتمرير إحداثيات GPS (العرض والطول) إلى RPC `check_in_with_token` بدلاً من الاعتماد solely على التحقق من الخادم عبر البذرة.
   2. تعديل RPC `check_in_with_token` في `actions.ts` لقبول معاملات `latitude` و `longitude` واستخدامها في التحقق من الجيوफ़نس (مع الحفاظ على التحقق من البذرة كأمان إضافي).
   3. تحديث تعريف RPC فيigrations إذا لزم الأمر (على الرغم من أن التغيير يكون في طبقة RPC فقط ولا يتطلب migration إذا لم يتغير الهيكل).
   4. اختبار السيناريوهات: داخل النطاق، خارج النطاق، بدون إذن الموقع. — المخاطر: رفض إذن الموقع من قبل المستخدم يؤدي إلى فشل الحضور؛ يجب توفير تجربة بديلة (مثل طلب الإذن مرة أخرى أو استخدام الكود الاحتياطي). — التراجع: إزالة معاملات الإحداثيات والعودة إلى النسخة السابقة من RPC ושاشة السكانر.
4. **تحسين توافق أبعاد المكونات مع不同 الشاشات (خاصة النص العربي)** — الملفات: جميع ملفات الشاشات في `src/features/` ومكونات التصميم في `src/design/components.ts` — الخطوات:
   1. مراجعة جميع القيم الثابتة للخط والمسافة والتحويل إلى نسبية أو مقيسة باستخدام `Dimensions` أو `useWindowDimensions` أو الخطوط المبنية على مقياس النظام.
   2. استخدام `react-native-safe-area-context` لضمان تجنب المناطق المحظورة (مثل النتوء والكاميرا).
   3. اختبار على مجموعة متنوعة من أحجام الشاشات (محاكي وأجهزة حقيقية إن أمكن) مع تكبير الخط لتجربة الخط العربي.
   4. الانتباه خصيصًا إلى مكونات النص التي قد تقطع الحروف العربية (تجنب `overflow: hidden` أو `numberOfLines={1}` للنصوص الطويلة). — المخاطر: إحداث تغييرات في التخطيط قد تؤثر على تجربة التصميم على الشاشات القياسية؛ يجب إجراء اختبار مرئي. — التراجع: الاحتفاظ بنسخة من الملفات المعدلة والعودة إلى القيم الثابتة إذا ظهرت مشاكل في التخطيط.
5. **تحسين تسميات التفاعلية للوصولية** — الملفات: جميع ملفات الشاشات في `src/features/` — الخطوات:
   1. إضافة خاصية `accessibilityLabel` descriptiva لجميع العناصر التفاعلية (الأزرار، الصور القابلة للنقر، إلخ).
   2. استخدام خاصية `accessibilityHint` عندما يكون الإجراء غير واضح من السياق.
   3. التأكد من أن التسميات مترجمة ومتكاملة مع نظام i18n (استخدام `t()` من `useI18n`).
   4. اختبار باستخدام أدوات فحص الوصولية (مثل TalkBack على Android وVoiceOver على iOS) إن أمكن. — المخاطر: إهمال بعض العناصر أو تقديم تسميات غير دقيقة؛ يتطلب مراجعة يدوية. — التراجع: إزالة خاصيات الوصولية المضافة والعودة إلى الحالة السابقة.
6. **إضافة تحديد معدل الخادم للـ RPCs الحساسة** — الملفات: src/data/actions.ts, وربما إنشاء ملف middleware أو استخدام مكتبة إذا كان متاحًا عبر Supabase Edge Functions — الخطوات:
   1. تحديد قائمة RPCs الحساسة (مثل `join_batch_by_code`, `check_in_with_token`, `uploadAvatar`, إلخ).
   2. تنفيذ آلية بسيطة لتحديد المعدل في طبقة RPC (مثل تخزين طوابع الوقت للمستخدم في الذاكرة المؤقتة أو في Redis إذا كنا نستخدم وظائف حافة، أو استخدام جدول في Supabase لتتبع المحاولات).
   3. إرجاع خطأ ودي (مثل `rate-limit-exceeded`) عندما يتجاوز المستخدم الحد المسموح به في نافذة زمنية.
   4. اختبار الحد من المعدل والسيناريوهات الحافة. — المخاطر: إضافة تعقيد قد يؤثر على الأداء أو يؤدي إلى حظر شرعي عن طريق الخطأ؛ يجب ضبط الحدود بعناية. — التراجع: إزالة منطق تحديد المعدل والعودة إلى النسخة السابقة من RPCs.
7. **إكمال إعداد دفع الإشعارات (تسجيل رمز الجهاز ومعالجة الواردات)** — الملفات: src/data/store.tsx, src/data/actions.ts, ربما shax في src/shared/push.ts — الخطوات:
   1. عند تسجيل الدخول أو التحديث، طلب إذن دفع الإشعارات وتسجيل رمز الجهاز في Expo Push.
   2. إرسال الرمز إلى الخادم عبر RPC جديدة (مثل `registerPushToken`) وتخزينه في جدول `push_tokens` (يجب التأكد من وجود الجدول والسياسات).
   3. عند تلقي إشعار دفع، معالجة في التطبيق (مثل عرض إشعار محلي أو تحديث 상태).
   4. اختبار الإشعارات في وضع التطوير والإنتاج. — المخاطر: فشل تسجيل الرمز أو معالجة الواردات قد يؤدي إلى فقدان الإشعارات؛ يجب معالجة الأخطاء وإظهار تغذية راجعة للمستخدم. — التراجع: إلغاء تسجيل الرمز وإزالة RPC אם נוספה والعودة إلى الحالة بدون دفع الإشعارات.

## أولوية متوسطة

### تحسينات الأداء العامة
— الملفات: جميع الملفات، خاصة celles التي تستخدم `useMemo` و `useCallback` و `React.memo` — الخطوات:
   1. مراجعة استخدام `useMemo` و `useCallback` للتأكد من أنها ضرورية ولا تسبب إعادة حساب مفرطة.
   2. إضافة `React.memo` للمكونات التي تُعاد render بشكل متكرر ولا تتغير خصائصها vaak.
   3. تحسين استعلامات Supabase (اختيار الحقول الضرورية فقط، استخدام الفهارس المناسبة).
   4. تشغيل أدوات تحليل الأداء (مثل Flipper أو Expo DevTools) لتحديد الاختناقات. — المخاطر: تحسينات غير ضرورية قد تزيد من التعقيد دون فائدة ملموسة. — التراجع: إزالة التحسينات والعودة إلى النسخة السابقة إذا أدى إلى ترجع في الأداء أو أخطاء.

### تحسينات التصميم وRTL
— الملفات: `src/design/` وجميع ملفات الشاشات — الخطوات:
   1. التأكد من استخدام رموز التصميم (`spacing`, `radii`, `sizes`) بشكل موحد وتجنب القيم الثابتة.
   2. اختبار التخطيط على أجهزة عربية واتجاه RTL لضمان انعكاس صحيح وتجنب القطع.
   3. مراجعة الخط IBM Plex Sans Arabic وضبط `textAlign` و `dir` و `wrapping` حسب الحاجة.
   4. تشغيل `/apple-design` للتحقق من الالتزام بمعايير أبل. — المخاطر: التغييرات البصرية قد لا تتوافق مع توقعات المستخدم؛ يجب جمع الملاحظات وإجراء اختبارات قابلية الاستخدام. — التراجع: العودة إلى القيم الثابتة أو التصميم السابق إذا ثبت أن الجديد غير مناسب.

### تحسينات الوصولية العامة
— الملفات: جميع ملفات الشاشات — الخطوات:
   1. تحسين تباين الألوان وفقًا لمعايير WCAG (نسبة 4.5:1 للنص العادي، 3:1 للعناوين الكبيرة).
   2. التأكد من أن هدف اللمس لا يقل عن 48x48 dp (أو استخدام اللمس القابل للوصول من مكونات التصميم).
   3. دعم قارئات الشاشة من خلال تقديم وصفات دقيقة وتجنب العناصر الزخرفية التي تُربك القارئ.
   4. اختبار مع مجموعة متنوعة من المستخدمين ذوي الإعاقات إن أمكن. — المخاطر: تحسين الوصولية قد يؤثر على الجماليات؛ يجب تحقيق توازن بين الوضوح والجاذبية البصرية. — التراجع: العودة إلى الألوان والأبعاد السابقة إذا ثبت أنها ضرورية لهوية العلامة التجارية.

### تنفيذ فعّال لدفع الإشعارات (المتابعة)
— الملفات: كما في البند 6 من الأولوية العالية — الخطوات: (متشابهة لكن مع التركيز على الموثوقية والتغطية الكاملة للمناسبات)
   1. تغطية جميع الأحداث التي تستدعي إشعارًا (مثل الحضور الغياب، ترقية المستوى، ottenere شارة، رسالة من المنظمة).
   2. السماح للمستخدم بضبط تفضيلات الإشعارات داخل التطبيق.
   3. معالجة حالات الفشل في التسجيل أو الاستقبال وإعادة المحاولة عند اللازم. — المخاطر: زيادة استهلاك البطارية أو استخدام البيانات؛ يجب تحسين تكرار الإشعارات. — التراجع: تقليل نطاق الإشعارات إلى الإ essencial فقط إذا لزم الأمر.

### validate فعالية جيوफ़نس
— الملفات: src/features/attendance/ScannerScreen.tsx وsrc/data/actions.ts — الخطوات:
   1. اختبار الجيوफ़نس في سيناريوهات واقعية (داخل范围, 外范围, على الحافة).
   2. تعديل نصف القطر أو طريقة الحساب (haversine) إذا لزم الأمر لتجنب الإيجابيات الكاذبة أو السلبيات الكاذبة.
   3. تسجيل محاولات الحضور لفحص الدقة وتحسين المعلمات. — المخاطر: تعديل المعلمات قد يؤدي إلى موازنة بين الدقة وتجربة المستخدم؛ يجب جمع البيانات واتخاذ قرار مستنير. — التراجع: العودة إلى نصف القطر أو طريقة الحساب السابقة إذا أثبتت الجديدة أنها أقل ملاءمة.

## أولوية منخفضة

### تحسين الكود (التسمية، توحيد التصدير)
— الملفات: جميع الملفات المصدرية — الخطوات:
   1. تطبيق قاعدة تسمية موحدة (camelCase للمتغيرات والوظائف، PascalCase للمكونات والنوعيات).
   2. توحيد آليات التصدير (التصدير المسمى مقابل التصدير الافتراضي) وتفضيل التسمية الم explícita.
   3. إزالة أي كود ميت أو متعطل باستخدام أدوات مثل `npm run lint` إن وُجد.
   4. تشغيل أوامر التحقق للتأكد من أن التحسينات لا تكسر شيئًا. — المخاطر: تغيير التسمية قد يسبب ارتباكًا مؤقتًا؛ يجب القيام به بحذر وفي فروع منفصلة. — التراجع: العودة إلى الأسماء الأصلية إذا تسبب في مشاكل في التكامل أو فهم الكود.

### تحسينات التوثيق
— الملفات: README.md، وإضافة توثيق داخلي حيث يلزم — الخطوات:
   1. تحسين README.md ليشمل شرحًا للمشروع، تعليمات الإعداد، ومساهمة المطورين.
   2. إضافة تعليقات توضيحية للوظائف المعقدة والمنطق غير البديهي.
   3. الحفاظ على تحديث ملفات التحليل السابقة (مثل AUDIT_COMPREHENSIVE_AR.md) بدلاً من إنشاء نسخ مكررة. — المخاطر: توثيق غير دقيق أو قديم قد يضلل المطورين الجدد. — التراجع: العودة إلى التوثيق السابق إذا оказался الجديد أقل فائدة.

### إزالة الكود الميت
— الملفات: جميع الملفات المصدرية — الخطوات:
   1. تحديد وإزالة المتغيرات والوظائف والوحدات التي لم تعد تُستخدم.
   2. استخدام أدوات مثل `git grep` أو إضافات المحرر للبحث عن المراجع.
   3. التأكد من أن الإزالة لا تؤثر على أي سلوك زمني حقيقي من خلال تشغيل الاختبارات. — المخاطر: إزالة شيء ما يُستخدم فعليًا عبر مسار غير واضح (مثل استدعاء ديناميكي)؛ يجب التحقق مرتين. — التراجع: استعادة الكود الميت من النسخة الاحتياطية إذا اكتشف أنه ضروري بعد الإزالة.